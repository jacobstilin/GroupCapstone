@model IEnumerable<ShapeShift.Models.Shift>


<table id= "MyShi" class="table" hidden>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.status)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.position)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.start)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.end)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.duration)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.additionalInfo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LocationId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UserId)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.status)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.position)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.start)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.end)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.duration)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.additionalInfo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LocationId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UserId)
                <button id="AddShiftToExchange" data-url='@Url.Action("AddMyShiftToExchange", "AppUsers", item.ShiftId)'>Add To Exchange</button>
            </td>
            <td>
            </td>
        </tr>
    }
</table>
<script>
     $(document).ready(function () {
       $('#AddShiftToExchange').click(function (evt) {
            evt.preventDefault();
            evt.stopPropagation();
            var $detailDiv = $('#EditShi'),
                url = $(this).data('url');
            $.get(url, function (data) {
                $detailDiv.replaceWith(data);
            });
       });
           });
</script>
     
