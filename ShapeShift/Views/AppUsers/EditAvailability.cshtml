@model IList<ShapeShift.Models.Availability>

@{
    ViewBag.Title = "EditAvailability";
}

<h2>EditAvailability</h2>

@using (Html.BeginForm()) 
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>Availability</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        

    <table class="table">
        <tr>
            <th>
                <h4>Week Day</h4>
            </th>
            <th>
                <h4>Start Time</h4>
            </th>
            <th>
                <h4>End Time</h4>
            </th>
            <th></th>
        </tr>

        @for(var i = 0; i < Model.Count(); i++)
        {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => Model[i].weekday)
        </td>
        <td>
            <div class="form-group">
                @Html.LabelFor(modelItem => Model[i].start, htmlAttributes: new { @class = "control-label col-md-2" })
                <div class="col-md-10">
                    @Html.EditorFor(modelItem => Model[i].start, new { htmlAttributes = new { @class = "form-control" } })
                    @Html.ValidationMessageFor(modelItem => Model[i].start, "", new { @class = "text-danger" })
                </div>
            </div>
        </td>
        <td>
            <div class="form-group">
                @Html.LabelFor(modelItem => Model[i].end, htmlAttributes: new { @class = "control-label col-md-2" })
                <div class="col-md-10">
                    @Html.EditorFor(modelItem => Model[i].end, new { htmlAttributes = new { @class = "form-control" } })
                    @Html.ValidationMessageFor(modelItem => Model[i].end, "", new { @class = "text-danger" })
                </div>
            </div>
        </td>


    </tr>
        }

    </table>

            

            <div class="form-group">
                <div class="col-md-offset-2 col-md-10">
                    <input type="submit" value="Create" class="btn btn-default" />
                </div>
            </div>
</div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
